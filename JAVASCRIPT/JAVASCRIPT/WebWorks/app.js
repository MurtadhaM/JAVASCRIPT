



// this function is for true or false questions that are unlimited

// values for each dropdown
let options = question.options;


function checkAnswer(answer, correctAnswer) {
    if (answer == correctAnswer) {
        return true;
    } else {
        return false;
    }
}
